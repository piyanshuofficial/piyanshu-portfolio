/** @type {import('next').NextConfig} */
const nextConfig = {
  // output: 'export', // Uncomment to enable static export (e.g. if switching away from Vercel standard deployment)
};

module.exports = nextConfig;
